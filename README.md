# AI Travel Concierge — Back-end

FastAPI back-end for an AI-powered travel concierge built with Claude.

## Stack
- Python + FastAPI
- Claude API (claude-haiku-4-5)
- SQLite + SQLAlchemy for conversation history
- Usage tracking with cost per message

## Setup
pip install -r requirements.txt
uvicorn main:app --reload
